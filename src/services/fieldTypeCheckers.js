export const isRadioInput = (element) => element.type === "radio";
export const isCheckboxInput = (element) => element.type === "checkbox";
export const isSelect = (element) => element.tagName.toLowerCase() === "select";
